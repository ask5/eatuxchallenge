/**
 * Created by abhi on 12/26/2015.
 */

$(document).ready(function () {

    /*
    $('.sticky').affix({
      offset: {
        top: 200,
        bottom: function () {
          return (this.bottom = $('footer').outerHeight(true))
        }
      }
    });
    */

    /*
    var sticky = $(".sticky");
    $(window).scroll(function(e){
        if ($(this).scrollTop() > 5) {
            sticky.addClass("sticky-content");
        } else {
            sticky.removeClass("sticky-content");
        }
    });
    */

});
